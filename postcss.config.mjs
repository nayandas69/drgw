/**
 * PostCSS Configuration for Discord Role Guardian Website
 * Uses Tailwind CSS v4 PostCSS plugin
 */

/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
}

export default config